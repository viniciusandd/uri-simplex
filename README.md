# Método Simplex

O programa simula a implementação do método Simplex.

## Como executar

Inicie um ambiente virtual.

`python3 -m venv venv`

Ative-o.

`source venv/bin/activate`

Instale as dependências.

`pip install -r requirements.txt`

Agora, basta executar a aplicação.

`python run.py`